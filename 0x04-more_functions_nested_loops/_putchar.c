#include "main.h"
#include <unistd>

/**
 * _putchar writes the characters c to stdout format
 * @ The character to be printed
 *
 * Return ON succes 1
 * on error -1 is returned
 */
int_putchar(char c)
(
 return(write(1, &C, 1))
)

